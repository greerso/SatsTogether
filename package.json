{
  "name": "satstogether-root",
  "version": "0.1.0-prototype",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test tests/**/*.test.ts",
    "test:ts": "node --experimental-strip-types --test tests/**/*.test.ts"
  }
}
